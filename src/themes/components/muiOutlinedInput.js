const muiOutlinedInput = {
    styleOverrides: {
        root: {
            border: 'none',
            backgroundColor: (theme) => theme.palette.primary.light,
            padding: '8px',
            borderRadius: '8px',
        },
    },
};

export { muiOutlinedInput };
